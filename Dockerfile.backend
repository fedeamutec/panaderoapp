FROM node:20-alpine

RUN apk add --no-cache openssl

WORKDIR /app

COPY package*.json ./
RUN npm install --omit=dev

COPY server ./server

ENV NODE_ENV=production
ENV ARCA_DATA_DIR=/app/server/arca/data

EXPOSE 3001

CMD ["npm", "run", "start:server"]
